
<br>
<br>
<br>
<div class="log-in container"></div>
<div class="container px-3">
<h2>Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <div class="">
    <% if devise_mapping.rememberable? %>
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    <% end %>
  </div>

  <%= f.button :submit, class: "btn-flat my-3", value:"Log In" %>
<% end %>

  <p>Default user credentials:</p>
  <p>Email: iratxe@skillswap.com</p>
  <p>Password: 123456</p>

<%= render "devise/shared/links" %>
</div>
</div>
